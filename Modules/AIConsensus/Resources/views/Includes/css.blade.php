<style>
.ai-consensus-page .ai-card{
    background: rgba(255,255,255,0.03);
    border: 1px solid rgba(255,255,255,0.08);
    border-radius: 14px;
    padding: 16px;
    margin-bottom: 16px;
}
.ai-consensus-page .ai-grid{
    display:grid;
    grid-template-columns: repeat(auto-fit,minmax(220px,1fr));
    gap:16px;
}
.ai-consensus-page .ai-stat-value{
    font-size:1.45rem;
    font-weight:700;
}
.ai-consensus-page .ai-muted{
    opacity:.75;
}
.ai-consensus-page textarea.ai-textarea{
    min-height: 280px;
    width: 100%;
}
.ai-consensus-page .ai-pre{
    white-space: pre-wrap;
    word-break: break-word;
    background: rgba(255,255,255,0.03);
    border-radius: 12px;
    padding: 14px;
    border: 1px solid rgba(255,255,255,0.06);
}
.ai-consensus-page .ai-badges{
    display:flex;
    flex-wrap:wrap;
    gap:8px;
}
.ai-consensus-page .ai-badge{
    display:inline-flex;
    align-items:center;
    padding:4px 10px;
    border-radius:999px;
    font-size:.85rem;
    border:1px solid rgba(255,255,255,0.12);
    background: rgba(255,255,255,0.04);
}
.ai-consensus-page .ai-actions{
    display:flex;
    flex-wrap:wrap;
    gap:10px;
}
.ai-consensus-page .table-responsive{
    overflow-x:auto;
}
.ai-consensus-page .ai-files-list{
    display:grid;
    gap:10px;
}
.ai-consensus-page .ai-provider-box{
    padding:12px;
    border-radius:12px;
    border:1px solid rgba(255,255,255,0.08);
    background:rgba(255,255,255,0.03);
}
</style>
